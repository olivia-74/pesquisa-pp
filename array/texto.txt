length: quantidade de itens
// push(elementos): adiciona um ou mais elementos no final de um array
// pop(): remove o último elemento do array
//splice(i,n): remove n de elementos a partir da posição i do array

// array: lista
// função: bloco de código que pode ser chamado a partir de um nome

const/let altura = 5
const/let largura = 8
function calcularArea(altura,largura){
    const area = altura * largura
    return area
}
console.log(calculaArea(altura,largura))
console.log(area)

console.log('X = ',(A + B));
console.log(`X = ${A + B}`);
console.log('X = ' + (A + B));

// objeto